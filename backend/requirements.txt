flask==3.0.3
flask-sqlalchemy==3.1.1
flask-cors==4.0.1
Pillow==10.3.0
numpy==1.26.4
gunicorn==21.2.0
psycopg2-binary==2.9.9

# ── AI / Computer Vision ───────────────────────────────────────────────────
# Uncomment the libraries you want to use:
#
# Full facial recognition (requires cmake + dlib build tools):
# face_recognition==1.3.0
# dlib==19.24.4
#
# Full pet pattern matching:
# opencv-python==4.9.0.80
#
# If neither is installed the app falls back to histogram similarity,
# which is fully functional for hackathon demos.
